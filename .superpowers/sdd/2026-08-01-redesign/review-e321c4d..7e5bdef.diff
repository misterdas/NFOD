# Review package: e321c4d..HEAD

## Commits
7e5bdef fix(styles): bounded table-scroll for sticky thead, date-chip border

## Files changed
 styles.css | 9 +++++++--
 1 file changed, 7 insertions(+), 2 deletions(-)

## Diff
diff --git a/styles.css b/styles.css
index 4063d23..7d89c84 100644
--- a/styles.css
+++ b/styles.css
@@ -151,23 +151,24 @@ button:disabled { cursor: default; }
 .btn-sm.btn-icon { width: 26px; height: 26px; padding: 2px; }
 
 .select {
   background: var(--surface-2); color: var(--text-1);
   border: 1px solid var(--border); border-radius: var(--radius-sm);
   padding: 5px 8px; font: 12px var(--font-sans);
 }
 .select:focus { border-color: var(--accent); outline: none; }
 
 .date-chip {
+  display: inline-block;
   font-family: var(--font-mono); font-variant-numeric: tabular-nums;
   min-width: 96px; text-align: center;
-  border-color: var(--border);
+  border: 1px solid var(--border);
   background: var(--surface-2);
 }
 .date-chip:hover { border-color: var(--border-strong); }
 
 /* ── KPI bar / cards ───────────────────────────────────────── */
 .kpi-bar {
   display: grid;
   grid-template-columns: repeat(auto-fit, minmax(190px, 1fr));
   gap: var(--space-4);
   margin-bottom: var(--space-5);
@@ -203,21 +204,25 @@ button:disabled { cursor: default; }
   overflow: hidden;
   box-shadow: var(--shadow-sm);
 }
 .block-header {
   background: var(--surface-3);
   padding: var(--space-2) var(--space-4);
   font-size: 10px; font-weight: 700; text-transform: uppercase;
   letter-spacing: .1em; color: var(--text-2);
 }
 
-.table-scroll { overflow-x: auto; }
+.table-scroll {
+  overflow-x: auto;
+  overflow-y: auto;
+  max-height: 420px;
+}
 
 .data-table { width: 100%; border-collapse: collapse; }
 .data-table th {
   background: var(--surface-3);
   font-size: 10px; font-weight: 700; text-transform: uppercase;
   letter-spacing: .07em; color: var(--text-3);
   text-align: right; padding: 8px 10px;
   position: sticky; top: 0; z-index: 3;
   white-space: nowrap;
 }
