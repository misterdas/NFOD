# Review package: 7e5bdef..HEAD

## Commits
12216e8 fix(styles): unclip table-scroll in print

## Files changed
 styles.css | 1 +
 1 file changed, 1 insertion(+)

## Diff
diff --git a/styles.css b/styles.css
index 7d89c84..19b346c 100644
--- a/styles.css
+++ b/styles.css
@@ -554,15 +554,16 @@ button:disabled { cursor: default; }
   body { background: #fff; color: #000; font-size: 10px; }
   * { box-shadow: none !important; text-shadow: none !important; }
   .view { display: block !important; animation: none !important; }
   .instrument-block, .kpi-card, .chart-card, .verdict-banner, .takeaway-item {
     border: 1px solid #000; background: #fff; border-radius: 0;
   }
   .data-table th, .data-table td {
     position: static !important; background: #fff !important; color: #000 !important;
     border: 1px solid #000 !important;
   }
+  .table-scroll { max-height: none; overflow: visible; }
   .data-table { border-collapse: collapse; }
   .badge, .pill, .status-pill, .pos-up, .pos-down, .verdict-gauge,
   .section-label, .breadth-head.up, .breadth-head.down { color: #000 !important; background: #fff !important; -webkit-text-fill-color: #000 !important; }
   .app-main { max-width: none; padding: 0; }
 }
